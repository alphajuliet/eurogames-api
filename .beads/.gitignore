# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Embedded-mode runtime state
.local_version
backup/
embeddeddolt/

# Keep JSONL exports and config (source of truth for git)
!*.jsonl
!metadata.json
!config.json
